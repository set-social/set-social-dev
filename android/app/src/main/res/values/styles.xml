<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <!-- Matches src/theme/tokens.ts colors.bg.base — this app is dark-mode-first
             with no light theme, so the native window background stays dark
             regardless of system setting, avoiding a white flash on cold start.
             launch_background.xml layers the brand mark on top of that same
             color, so cold start shows the real splash (matching iOS's
             LaunchScreen.storyboard) instead of a blank frame. -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>

</resources>
